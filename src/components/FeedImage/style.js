import { StyleSheet } from 'react-native';

export const small = StyleSheet.create({
    width: '100%',
    background: '#eee',
});

export const Original = StyleSheet.create({
    width: '100%',
});
